/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  distDir: "docs",
  // basePath: "/SEU_REPOSITORIO",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
